/**
 * football-api.js — Stubs en attente de l'agent IA
 *
 * L'intégration RapidAPI a été retirée.
 * Ces fonctions seront branchées sur l'agent IA de scraping.
 */

// Recherche de joueur — sera remplacé par l'agent IA
export async function searchPlayer(query) {
  return [];
}

// Compositions — sera remplacé par l'agent IA
export async function getLineups(matchId) {
  return null;
}

// Head-to-head — sera remplacé par l'agent IA
export async function getHeadToHead(team1, team2) {
  return null;
}

// Polling live — sera remplacé par l'agent IA
export function startLivePolling() {
  return null;
}
